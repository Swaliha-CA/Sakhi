# Core Framework (Required for Vercel)
fastapi==0.115.0
uvicorn==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-multipart==0.0.12

# Database (Required - SQLAlchemy for ASHA endpoints)
sqlalchemy==2.0.36

# HTTP Client
httpx==0.27.2
requests==2.32.3

# Utilities
python-dotenv==1.0.1

# Image Processing (Lightweight for Vercel)
Pillow==11.0.0

# Note: Heavy dependencies removed for Vercel deployment
# PaddleOCR, OpenCV, Redis, and testing libraries are excluded
# to keep the serverless function size under Vercel's 50MB limit
# OCR functionality uses mock data in production
